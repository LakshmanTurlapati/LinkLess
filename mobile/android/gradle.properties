org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Mapbox SDK download authentication.
# The SDK_REGISTRY_TOKEN must be set in ~/.gradle/gradle.properties (NOT here).
# Create a Mapbox secret token with Downloads:Read scope at:
# https://console.mapbox.com/account/access-tokens/
# Then add to ~/.gradle/gradle.properties:
#   SDK_REGISTRY_TOKEN=sk.your_secret_token_here
